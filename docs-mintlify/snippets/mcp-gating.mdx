Mutating tools carry the `destructiveHint` annotation, so a compliant client
(Claude Code, Claude Desktop, claude.ai) shows a native Allow/Deny prompt before
running them. Nothing changes without your confirmation.
